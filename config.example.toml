# RustyClaw Configuration Example
# Copy this to ~/.rustyclaw/config.toml and customize

# Directory for RustyClaw settings and data
settings_dir = "/home/user/.rustyclaw"

# Path to SOUL.md file (optional)
# soul_path = "/home/user/.rustyclaw/SOUL.md"

# Directory containing skills (optional)
# skills_dir = "/home/user/.rustyclaw/skills"

# Whether to use the secrets storage system
use_secrets = true

# Whether to route LLM calls through a local codex-acp subprocess.
# false = use the current provider HTTP APIs.
# true  = use codex-acp over stdio JSON-RPC.
use_codex_acp = false

# Blank lines between messages in the TUI (0 = compact, 1 = comfortable)
# message_spacing = 1

# TLS configuration for WSS (WebSocket Secure) gateway connections.
# Both tls_cert and tls_key must be set to enable WSS.
# tls_cert = "/path/to/cert.pem"
# tls_key = "/path/to/key.pem"

# Messenger configurations
# [[messengers]]
# name = "slack"
# messenger_type = "slack"
# enabled = false
# token = "xoxb-your-slack-bot-token"
# allowed_chats = ["C0123456789"] # Slack channel IDs to poll
# allowed_users = []              # Optional Slack user ID allow-list

# [[messengers]]
# name = "discord"
# messenger_type = "discord"
# enabled = false
# token = "discord-bot-token"
# allowed_chats = []
# allowed_users = []

# Messenger pipeline tuning:
# messenger_poll_interval_ms = 2000     # Poll cadence, clamped to >= 500ms
# messenger_worker_count = 4            # Concurrent workers
# messenger_queue_capacity = 256        # Bounded inbound queue size
# messenger_enqueue_timeout_ms = 200    # Per-attempt enqueue timeout (2 attempts)
